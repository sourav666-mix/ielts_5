# ==============================================================
# ATLAS IELTS Academy — backend dependencies
# Compatible-release pins (~=): patches flow, breaking changes don't.
# The Dockerfile installs THIS file; pyproject.toml is tooling/metadata only.
# ==============================================================

# ── Core API ─────────────────────────────────────────────────
fastapi~=0.115
uvicorn~=0.32
python-multipart~=0.0.17        # FastAPI Form/File — required by /writing/grade
                                 # (multipart §6.5) and /stt (audio §13.4)

# ── Persistence ───────────────────────────────────────────────
#   NO database: a JSON file store (store.py) — stdlib only.

# ── Config & validation ──────────────────────────────────────
pydantic~=2.9
pydantic-settings~=2.6

# ── Auth ─────────────────────────────────────────────────────
python-jose[cryptography]~=3.3  # JWT + a crypto backend (jose needs one)

# ── AI providers (OpenRouter + Groq, spec §12) ───────────────
httpx~=0.28